{
  "name": "k2-railgun-turret",
  "version": "1.0.1",
  "description": "Railgun Turrent from Krastorio 2.",
  "author": "RaptorTWiked",
  "license": "ISC",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "copy-lang": "rm -rf locale && ts-node scripts/copy-lang.ts",
    "build": "rm -rf build && /bin/bash scripts/build.sh",
    "read-info": "ts-node scripts/read-info.ts"
  },
  "devDependencies": {
    "ts-node": "^10.9.1"
  },
  "dependencies": {
    "configparser": "^0.3.10"
  }
}
